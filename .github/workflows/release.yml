name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build .skill artifact
        run: |
          bash skills/peter-wanna-know/scripts/build-skill.sh
          test -f dist/peter-wanna-know.skill

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/peter-wanna-know.skill
          generate_release_notes: true
          draft: false
          prerelease: false
